import { NavLink, Link } from 'react-router';

const Logo = '/logo.png';

const Navbar = () => {
    return (
        <header className="container font-lato fixed inset-x-0 top-0">
            Navbar
        </header>
    );
};

export default Navbar;
